# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches:
      - main

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: ${{ secrets.GCP_REGION }}
  GOOGLE_DEFAULT_CALENDAR_ID: ${{ secrets.GOOGLE_DEFAULT_CALENDAR_ID }}
  GOOGLE_APPSHEET_APP_ID: ${{ secrets.GOOGLE_APPSHEET_APP_ID }}
  GCP_GOOGLE_CALENDAR_SERVICE_ACCOUNT_EMAIL: ${{ secrets.GCP_GOOGLE_CALENDAR_SERVICE_ACCOUNT_EMAIL }}
  HEADER_SOURCE_TO_PASS: ${{ secrets.HEADER_SOURCE_TO_PASS }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v0.2.0
      with:
        version: 'latest'
        project_id: ${{ env.GCP_PROJECT_ID }}
        service_account_key: ${{ secrets.GCP_SA_KEY }}

    - name: Install Terraform
      run: |
        sudo apt-get update && sudo apt-get install -y terraform

    - name: Terraform Init
      run: terraform init

    - name: Terraform Apply
      run: terraform apply -auto-approve
      env:
        GOOGLE_APPSHEET_ACCESS_KEY: ${{ secrets.GOOGLE_APPSHEET_ACCESS_KEY }}
        GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}